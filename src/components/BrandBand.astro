---
import Marquee from './Marquee.astro';
import { brands, getContent } from '../data/content';
const { ui } = getContent(Astro.currentLocale);
---

<section aria-label={ui.brandsAria} class="relative overflow-hidden border-y border-racing/10 bg-paper py-14 sm:py-16">
  <div class="mx-auto mb-8 flex max-w-[1440px] items-center gap-4 px-6 sm:px-10 lg:px-14" data-reveal="rise">
    <span class="h-px w-10 bg-gold-dark/50"></span>
    <span class="eyebrow">{ui.brandsEyebrow}</span>
  </div>

  <Marquee
    items={brands}
    duration={70}
    separator="—"
    class="font-serif text-[clamp(2rem,5vw,4rem)] font-light leading-none text-racing/85"
  />
  <div class="mt-3">
    <Marquee
      items={brands}
      duration={85}
      reverse
      separator="—"
      class="font-serif text-[clamp(2rem,5vw,4rem)] font-light italic leading-none text-racing/25"
    />
  </div>
</section>
